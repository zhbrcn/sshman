# sshman 快速启动

在 Linux 系统中，一键执行该脚本可以使用下面的命令（需有 Bash 环境并且具备足够权限）：

```
bash sshman.sh
```

也可以直接通过 `curl` 下载脚本、授予可执行权限并运行，使用一条命令完成：

```
curl -fsSL https://raw.githubusercontent.com/zhbrcn/sshman/main/sshman.sh -o sshman.sh && chmod +x sshman.sh && ./sshman.sh
```
